<title>Mein Blogg </title>

<h1><%=current_user.nickname %>s Blogg</h1>


    <% @posts.each do |post| %>
    <br />
     <br />
      
      
      <h3><%= post.title %></h3>
        <%= post.content %>
       <%= link_to 'Show', post %>
        <%= link_to 'Edit', edit_post_path(post) %>
       <%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %>
      <% end %>

<br />
<br />
<br />
<%= link_to 'New Post', new_post_path %>


